<%= render "shared/second_header" %>
<div class="contents row">
  <div class="content_post">
    <div class="more">
      <ul class="more_list">
        <li>
          <%= link_to '記録を編集', edit_training_menu_menu_record_path(@menu_record.id), method: :get %>
        </li>
        <%# <li> %>
          <%# <%= link_to '削除', training_menu_path(@training_menu.id), method: :delete, data: {confirm: "削除しますか？"} %>
        <%# </li> %>
     </ul>
    </div>
    <h2><%= @menu_record.date %></h2>
    <h3><%= @menu_record.form_level.name %></h3>
  </div>
  <div class="contents row">
        <% unless @menu_record.weight == nil %>
          <h2><%= @menu_record.weight %>kg（両手）</h2>
        <% end %>
        <% unless @menu_record.reps == nil %>
          <h2><%= @menu_record.reps %>回 ※.の後に続く回数はチーティングの回数</h2>
        <% end %>
        <% unless @menu_record.circuit_menus == nil %>
          <h2><%= @menu_record.circuit_menus %></h2>
        <% end %>
        <% unless @menu_record.memo == nil %>
          <h2><%= @menu_record.memo %></h2>
        <% end %>
  </div>
  
</div>
<%= render "shared/footer" %>